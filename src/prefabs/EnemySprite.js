export default class EnemySprite{

    static sayName(){
        let EnemyName;
        if(Math.random() > 0.9)
            EnemyName = 'etin';
            
        return EnemyName;
    }

}